{
  "name": "tslox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run ast:generate && tsc ./*.ts",
    "start": "npm run build && node ./index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ast:generate": "tsc ./scripts/generate-ast.ts && node ./scripts/generate-ast.js .",
    "console": "npm run build && node"
  },
  "author": "Josh Holbrook",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "read": "^3.0.0"
  }
}
